<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coursework2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CourseWork2">
        <activity android:name=".RatingsOfOut"></activity>
        <activity android:name=".Ratings_In" />
        <activity android:name=".UpdateforActivity" />
        <activity android:name=".SearchforMovie" />
        <activity android:name=".MovieEdit" />
        <activity android:name=".EditRecord" />
        <activity android:name=".DisplayActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>